
(function (angular) {
    'use strict';

    angular.module('translatorProject.dashboard', []).config(Configure);

    Configure.$inject = ['$routeProvider'];
    function Configure($routeProvider) {
              
    }

})(window.angular);