
(function (angular) {
    'use strict';

    angular.module('translatorProject.application', []).config(Configure);
    Configure.$inject = ['$routeProvider'];

    function Configure($locationProvider, $routeProvider) {

    }

})(window.angular);